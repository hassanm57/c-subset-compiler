.\compiler.exe -tac -no-opt demo.c   ← show raw TAC (THREE ADDRESS CODE) (skips opt)
.\compiler.exe -opt demo.c           ← show optimized TAC (cleaner, fewer steps)
.\compiler.exe -ast demo.c           ← show the full syntax tree
.\compiler.exe -sym demo.c           ← show symbol table with all names

.\compiler.exe -tac demo.c ---- shows code before optimization
.compiler.exe -opt demo.c --- shows code after optimzation

// btw
compiler.exe -o myfile.tac demo.c // write output to any custom file

USEFUL FLAGS:

do .\compiler.exe (any of the flags from above - skip these parantheses) <file-name>

// take from any of the test files from tests folders.
